Place the default project icon image here as `default-app.png`.

Expected path (used by the renderer):

assets/default-app.png

Recommended: a square PNG (256x256 or larger) to look crisp in the UI.


